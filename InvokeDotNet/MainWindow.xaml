<Window x:Class="InvokeDotNet.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InvokeDotNet"
        mc:Ignorable="d"
        Title="Invoke.NET" Height="450" Width="800">
    <Grid>
        <DockPanel LastChildFill="True">
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="File">
                    <MenuItem Header="New"/>
                    <MenuItem Header="Open"/>
                    <Separator/>
                    <MenuItem Header="Close"/>
                    <Separator/>
                    <MenuItem Header="Exit"/>
                </MenuItem>
                <MenuItem Header="Help">
                    <MenuItem Header="About"/>
                </MenuItem>
            </Menu>
            <Grid DockPanel.Dock="Bottom" Margin="3">
                <WrapPanel>
                    <TextBlock x:Name="tbStatus">Ready</TextBlock>
                </WrapPanel>
            </Grid>
            <Grid DockPanel.Dock="Left" MinWidth="150">
                <local:DockAssemblies x:Name="dAsms"/>
            </Grid>
            <Grid x:Name="MainContentGrid">
                
            </Grid>
        </DockPanel>
    </Grid>
</Window>
